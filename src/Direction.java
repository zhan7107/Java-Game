/**
 * Direction is a enum store the constants for the move methods. Use for Board and Mobile Class.
 * 
 * @author Hongli Zhang
 */
public enum Direction {
	UP, DOWN, LEFT, RIGHT, UP_LEFT, UP_RIGHT, DOWN_LEFT, DOWN_RIGHT
}
